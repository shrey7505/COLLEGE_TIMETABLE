<% layout("/layouts/boilerplat") %>
<body> 
    
<div class="faculty-edit-container body1">
    
    <h2 class="faculty-edit-title">Edit Faculty Details</h2>
    <form method="post" action="/itlisting/<%= itdata._id %>?_method=PUT" enctype="multipart/form-data" class="faculty-edit-form">
        
        <!-- Name -->
        <div class="form-group">
            <label for="name" class="form-label">Name:</label>
            <input type="text" id="name" name="itlisting[name]" required value="<%= itdata.name %>" class="form-input">
        </div>

        <!-- Designation -->
        <div class="form-group">
            <label for="designation" class="form-label">Designation:</label>
            <input type="text" id="designation" name="itlisting[designation]" required value="<%= itdata.designation %>" class="form-input">
        </div>

        <!-- Qualifications -->
        <div class="form-group">
            <label for="qualification" class="form-label">Qualifications:</label>
            <textarea id="qualification" name="itlisting[qualification]" rows="3" required class="form-textarea"><%= itdata.qualification %></textarea>
        </div>

        <!-- Academic Experience -->
        <div class="form-group">
            <label for="academics" class="form-label">Academic Experience (Years):</label>
            <input type="number" id="academics" name="itlisting[experience][academics]" required value="<%= itdata.experience.academics %>" class="form-input">
        </div>

        <!-- Industry Experience -->
        <div class="form-group">
            <label for="industry" class="form-label">Industry Experience (Years):</label>
            <input type="number" id="industry" name="itlisting[experience][industry]" required value="<%= itdata.experience.industry %>" class="form-input">
        </div>

        <!-- Emails -->
        <div class="form-group">
          <label for="emails" class="form-label">Emails:</label>
          <input type="email" id="emails" name="itlisting[contact][emails]" 
                 value="<%= itdata.contact.emails ? itdata.contact.emails.join(', ') : '' %>"
                 class="form-input" multiple>
      </div>

        <!-- WhatsApp -->
        <div class="form-group">
            <label for="whatsapp" class="form-label">WhatsApp Number:</label>
            <input type="text" id="whatsapp" name="itlisting[contact][whatsapp]" required value="<%= itdata.contact.whatsapp %>" class="form-input">
        </div>

        <!-- Image URL -->
        <!-- <div class="form-group">
            <label for="image" class="form-label">Profile Image URL:</label>
            <input type="text" id="image" name="itlisting[image]" value="<%= itdata.image %>" class="form-input">
        </div> -->
        <div class="form-group">
          <label for="image" class="form-label">Uplode Image:</label>
          <input type="file" id="image" name="itlisting[image]" class="form-input">
      </div>

        <label class="form-label taught-subjects-label">Subjects Taught:</label>
        <div class="taught-subjects-wrapper">
          <% semesters.forEach((semester) => { %>
            <div class="semester-section">
              <h4 class="semester-title">Semester <%= semester.semester %></h4>
              <div class="subjects-grid">
                <% semester.courses.forEach((course) => { %>
                  <div class="subject-checkbox-item">
                    <input 
                      type="checkbox" 
                      id="subject-<%= course.subject %>" 
                      name="itlisting[subjects][]" 
                      value="<%= course.subject %>" 
                      <% if (itdata.subjects && itdata.subjects.includes(course.subject)) { %>checked<% } %>
                    >
                    <label for="subject-<%= course.subject %>"><%= course.subject %></label>
                  </div>
                <% }) %>
              </div>
            </div>
          <% }) %>
        </div>
        
        
            

        <!-- Submit Button -->
        <div class="form-group">
            <button type="submit" class="form-button">Update Details</button>
        </div>

        
    </form>
</div>

</body>
