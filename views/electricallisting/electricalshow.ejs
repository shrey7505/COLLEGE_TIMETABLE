<% layout("/layouts/boilerplat") %>
<body>
    
  <div class="faculty-card">
    <h2 class="faculty-name"><%= electricaldata.owner.username %></h2>
    <h2 class="faculty-name"><%= electricaldata.name %></h2>
    <h3 class="faculty-title"><%= electricaldata.designation %></h3>

    <!-- Profile Image -->
    <img src="<%= electricaldata.image.url %>" alt="Profile Picture" class="faculty-image" />

    <!-- Experience -->
    <p class="faculty-info">
        <strong>Academic Experience:</strong> <%= electricaldata.experience.academics %> years
    </p>
    <p class="faculty-info">
        <strong>Industry Experience:</strong> <%= electricaldata.experience.industry %> years
    </p>

    <!-- Qualifications -->
    <p class="faculty-info"><strong>Qualifications:</strong></p>
    <ul class="faculty-list">
        <% electricaldata.qualification.forEach(function(q) { %>
            <li><%= q %></li>
        <% }); %>
    </ul>

    <!-- Emails -->
    <p class="faculty-info"><strong>Emails:</strong></p>
    <ul class="email-list faculty-list">
      <% 
        let emails = electricaldata.contact.emails; 
        if (typeof emails === 'string') {
          emails = emails.split(',');
        }
        emails.forEach(function(email) { 
      %>
        <li><a href="mailto:<%= email.trim() %>" class="contact-link"><%= email.trim() %></a></li>
      <% }); %>
    </ul>

    <!-- WhatsApp Contact -->
    <p class="faculty-info">
        <strong>WhatsApp:</strong> 
        <a href="https://wa.me/<%= electricaldata.contact.whatsapp %>" target="_blank" class="contact-link whatsapp-contact">
            <%= electricaldata.contact.whatsapp %>
        </a>
    </p>

    <!-- Subjects Taught -->
    <p class="faculty-info"><strong>Subjects Taught:</strong></p>
    <ul class="faculty-list">
      <% if (electricaldata.subjects && electricaldata.subjects.length > 0) { %>
        <% electricaldata.subjects.forEach(function(subject) { %>
          <li><%= subject %></li>
        <% }); %>
      <% } else { %>
        <li>No subjects listed.</li>
      <% } %>
    </ul>
    
    <% if (currentUser && currentUser._id.toString() === "67ff31a54b2d29d2457265b1") { %>

      <div>
        <!-- Action Buttons -->
        <a href="/electricallisting/<%= electricaldata._id %>/edit" class="action-link">EDIT</a>

        <form method="post" action="/electricallisting/<%= electricaldata._id %>?_method=DELETE" class="delete-form" style="display:inline;">
            <button type="submit">DELETE</button></div>
        </form>
      </div>
    
    <% } %>

</body>
